import React from 'react'

const ButtonBigMoney = (props) => {

    return (
      
       <button id="startGame" onClick={props.handleStart}>Start Game</button> 
    
    )
}

export default ButtonBigMoney;